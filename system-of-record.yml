  gsvsoc-system-of-record:
    container_name: gsvsoc-system-of-record
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "514:514/udp"
      - "601:601"
    volumes:
      - /logs/HOSTS:/logs/HOSTS:rw
    restart: always
    networks:
      - elastic-network