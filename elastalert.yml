    elastalert:
        image: bitsensor/elastalert:latest
        environment:
            ELASTIC_PWD: "CHANGE_ME"
            ELASTICSEARCH_HOST: elastic-container-1
            ELASTICSEARCH_PORT: 9200
        volumes:
            - /etc/elastalert:/etc/elastalert
            - /usr/share/elastalert/rules:/usr/share/elastalert/rules
        network_mode: bridge
        restart: unless-stopped